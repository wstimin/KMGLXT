# 十夜卡密 · 综合性卡密管理系统方案

> 版本 v1.2 · 待审阅 · 未开始编码
> 变更记录:按你的要求推翻 v0.1 重做 ——
> **① 卡密改为"全局统一池 + 一卡通用":** 不再每项目独立卡池,所有卡进同一个池,顾客在任意项目里都能用,激活时归属该项目,一卡只核销一次。
> **② UI 升级为「水晶玻璃 · 光晕蓝紫」:** 浅彩磨砂玻璃 **+ 淡彩背景(绝不纯白)** + 青→紫渐变 + 霓虹光晕,**真实性渐变立体图标**,大气的留白与 hero 大数字,炫酷但不黑。
> **③ 你的项目均为网站**:SDK 示例以 PHP 优先,附 Node / Python / curl。
> 完整规划见 [规划书.md](规划书.md)。

---

## 1. 一句话定位

把 N 个项目的卡密统一起来:**一个全局卡池,所有卡通用**,顾客在哪个项目用就在哪个项目激活;旧项目的卡也能并进来 —— 生成、管理、接管、对外验卡,全在十夜一个系统。

---

## 2. 核心模型:全局统一卡池(重点)

```
        ┌─────────────────────────────────────┐
        │          十夜·全局统一卡池              │
        │  ┌──────┐ ┌──────┐ ┌──────┐ ...     │
        │  │ 月卡  │ │ 终身卡 │ │ 10次卡 │        │  ← 卡不属于任何项目
        │  └──────┘ └──────┘ └──────┘          │      属于"全局"和"套餐"
        └──────┬──────────────┬───────────────┘
               │ 顾客任选      │
        ┌──────▼─────┐  ┌─────▼──────┐  ┌──────▼─────┐
        │  项目A      │  │  项目B      │  │  项目C ...   │
        │ (验卡/激活) │  │ (验卡/激活) │  │            │
        └────────────┘  └────────────┘  └────────────┘
       所有项目用同一套 API、同一个池 —— 这才是"统一使用"
```

**与 v0.1 的关键差异:**

| | v0.1(你否掉的) | v1.1(现在的) |
|---|---|---|
| 卡池 | 每项目独立卡池 | **一个全局池**,所有项目共用 |
| 卡与项目 | 生成时绑定项目 | **生成时不绑定任何项目** |
| 使用 | 项目只能用自己的卡 | **任意项目的顾客都能用同一张卡**,激活时归属该项目 |
| 套餐 | 分项目定义 | **全局定义**,可限定"只允许哪些项目使用"(默认全通用) |
| 旧卡接管 | 按项目导入 | **统一导入全局池**,可选打上来源标记 |

**一张卡的生命周期:**

```
生成(全局池,未使用)
   │ 顾客在项目X里输入卡号并激活
   ▼
激活校验通过 → 状态"已使用",绑定归属项目=X,开始计时/计次
   │ 时效/次数耗尽
   ▼
过期/耗尽 → 状态"已过期"
中途可被管理员"禁用 / 作废 / 恢复"
```

**一卡只核销一次。** 激活后,再拿这张卡去别的项目验卡 → 系统返回"该卡已绑定项目X,无法在其他项目使用",防止一张卡到处刷。

---

## 3. 使用流程(三套视角)

**顾客视角:** 买了月卡 → 在项目A输入卡号 → 激活 → 开始在A用;想给项目B用?同一张卡不行,买新卡就行。

**老板视角:** 所有渠道卖的卡都在一个池子里;打开仪表盘看总量、使用率、各项目核销占比,一目了然;随时批量生成、禁用、导出。

**项目接入视角(接管):**
1. 后台新建项目 → 拿 `app_key / app_secret`
2. 项目的验卡代码换成调十夜 API(只改一处)
3. 旧卡密打包成 TXT/CSV → 导入全局池(可选打来源标记)
4. 测试 → 上线 → 旧系统退役

---

## 4. 功能模块

### 4.1 全局卡池 / 卡密管理
- 列表:卡号、套餐、状态、**归属项目**(未使用的为空,激活后显示)、创建时间、激活时间、IP
- 状态:`未使用 / 已使用 / 已过期 / 已禁用 / 已作废`
- 操作:禁用/启用、作废、重置(已使用回退成未使用)、批量导出、批量删除
- 多维筛选:套餐、状态、归属项目、时间范围 + 关键词搜索
- **重点:列表本身就是一张"全局总表",所有项目的卡混在一起管理**

### 4.2 卡密生成
- 批量生成:数量、自定义前缀、位数、字符集(剔除易混字符 `0O1lI`)
- 指定套餐、批次备注
- 防碰撞:全局唯一索引 + 去重检测,可选校验位(防瞎猜卡号)
- 生成批次日志

### 4.3 套餐(卡密类型)管理 —— 全局定义
- 例:「7天卡」「30天卡」「永久卡」「10次体验卡」
- 每套餐:名称、类型(**时长**天数 / **次数** / 永久)、价格(可选)、**适用范围**(默认"全部项目",也可勾选只让指定项目用)
- 顾客拿起一张卡,能用在哪些项目 = 看套餐的适用范围

### 4.4 项目管理 —— 只管"接入授权",不绑卡池
- 项目:名称、`app_key`、`app_secret`(支持重置轮换)、状态、备注、回调地址、IP 白名单(可选)
- 卡池是全系统共用的,项目这里只负责"谁能进来验卡"

### 4.5 旧卡导入(接管)
- 导入 TXT/CSV 到**全局池**:一行一卡,或 卡号+状态+套餐+备注
- 全局去重:冲突自动跳过并报告
- 可选带"来源项目"标记,方便过渡期统计
- 交接工具:新生成的一键导出,和旧系统的导出格式对接

### 4.6 对外验卡 API(统一语义,语言无关)
| 接口 | 作用 |
|---|---|
| `POST /api/v1/card/verify` | 验卡:卡是否有效,返回剩余时长/次数 |
| `POST /api/v1/card/activate` | 激活核销:校验通过→归本项目,开始计时/计次 |
| `POST /api/v1/card/consume` | 扣次数(次数卡) |
| `POST /api/v1/card/query` | 查卡详情/余额/归属项目 |
| `POST /api/v1/card/freeze` / `unfreeze` | 冻结/解冻 |

- 统一响应 `{ code, message, data }` + 错误码字典
- 鉴权:`app_key + app_secret + 时间戳 + nonce` 签名,防重放防伪造
- 附 SDK 示例:**PHP / Python / Node / 易语言 / curl**,你项目什么语言抄对应的
- 核销流水全量记录:哪张卡、哪个项目、IP、时间

### 4.7 统计与看板
- 全局维度:总量、未使用、已使用、过期、禁用、今日生成/核销
- 项目维度:各项目核销占比、客单趋势
- 近 7/30 天"生成 vs 消耗"渐变面积图

### 4.8 系统设置
- 多管理员、登录失败锁定、可选验证码
- SQLite 一键备份/下载/恢复
- 站点信息与公告

---

## 5. 技术选型

| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | **Node.js + Express** | 与前端同语言,API 密集场景高性能,pm2 上云稳 |
| 前端 | **Vue3 + Vite + Element Plus** | 组件全,二次定制精美主题有成熟路子 |
| 数据库 | **SQLite + better-sqlite3** | WAL 模式、事务安全、单文件免部署 |
| 部署 | pm2 常驻 + Nginx 反代 HTTPS | Vite 打包后的静态文件由后端直接托管,单进程跑全站 |

---

## 6. UI 设计规范 ——「水晶玻璃 · 光晕蓝紫」

### 6.1 设计总纲
> **淡彩通透打底,水晶玻璃承载信息,青→紫渐变与霓虹光晕负责炫酷。** 氛围感来自"光",而不是"黑"或"纯白"。全程**淡彩浅色系**(带蓝紫微光的底色,不是 #ffffff),越往深处理越克制,绝不出纯黑背景。

### 6.2 配色表(精确到值)
| 角色 | 色值 | 用途 |
|---|---|---|
| 页面底色 | `#eef1ff → #e3ecff` 斜向渐变 | **淡彩背景,带蓝紫微光,绝不纯白** |
| 主渐变 | 青 `#22d3ee` → 蓝 `#4f7cff` → 紫 `#8b5cf6` | Logo、按钮、顶栏光条、图表 |
| 霓虹光晕 | `#38bdf8` / `#a78bfa` / `#fb7185` 三个柔光斑 | 面板背后浮动的光,blur 大光圈 |
| 玻璃面 | `rgba(255,255,255,.66)` + `backdrop-filter: blur(20px)` | 卡片/顶栏/弹窗,1px 白边 |
| 主文字 | `#1e2b4f`(深蓝墨) | 标题、正文 |
| 次文字 | `#64748b` | 说明、表头 |
| 成功/警告/危险 | `#10b981` / `#f59e0b` / `#ef4444` | 状态药丸、提示 |
| 状态药丸 | 渐变小底色 + 同色系文字 | 未使用(青)、已使用(蓝)、过期(灰紫)、禁用(红) |

### 6.3 质感与光效
- **玻璃卡片**:半透明白 + 磨砂 + 1px 白描边 + 柔和彩色投影(`0 8px 32px rgba(79,124,255,.12)`)
- **边缘渐变描边**:重点卡片用"渐变边"——青→紫 1px 渐变 border 勾勒
- **背景光斑**:页面上方三团大柔光(青/紫/粉)缓慢漂移,`filter: blur(80px)`,营造"被光笼罩"的幻觉
- **按钮**:大圆角,底色走主渐变,悬停时光晕扩散 + 轻微上浮

### 6.4 真实性图标规范
- **不用细线条扁平图标**。统一使用**渐变立体质感图标**:双色渐变填充(青蓝→紫)+ 微浮雕高光 + 柔和彩色投影,接近"真实、有质感"
- 全部 SVG(任何 DPI 都清晰),封装成 `<Icon>` 组件,统一渐变角度与线宽
- 覆盖:导航模块图标 ×9、数据卡图标、状态徽标、操作图标;hover 上浮发光
- 不是从网上下载散装图标,而是**与主题统一的一套渐变图标组件库**,保证整体风格一致

### 6.4 页面布局(靶子)
```
登录页
┌────────────────────────────────────────────┐
│    ✦ 渐变光斑背景(青→紫,动态漂移)             │
│                                              │
│        十夜卡密  (毛玻璃大牌字)                 │
│   「统一 · 接管 · 炫丽」一小句 slogan           │
│         ┌──────────────┐                     │
│         │  玻璃登录卡   │  ← 磨砂白 + 渐变光边  │
│         │ 账号 │ 密码  │                     │
│         │ [ ── 渐变按钮 ── ]                 │
│         └──────────────┘                     │
└────────────────────────────────────────────┘

仪表盘
┌────────────────────────────────────────────┐
│ 十夜卡密  ▌仪表盘 卡密 生成 套餐 项目 日志 ⚙  │ ← 玻璃顶栏,选中项渐变条
│ ┌──────────────────────────────────────────┐│
│ │  今日核销 1,024 ▲12%   在线项目 6          ││ ← hero 大数字,滚动动效
│ │  总卡密 58,204   使用率 78% [━━━━━━◇]    ││   光晕发光卡片带渐变
│ └──────────────────────────────────────────┘│
│  ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │ 生成趋势 │ │ 项目占比 │ │ 动态流水 │       │ ← 玻璃卡片网格
│  └─────────┘ └─────────┘ └─────────┘       │
└────────────────────────────────────────────┘
```

### 6.5 动效目录(克制但到位)
- 统计数字**滚动递增**;卡片 **hover 上浮 + 光晕放大**
- 背景光斑**缓慢漂移**(10s 循环);渐变描边呼吸
- 页面切换淡入,弹窗以磨砂遮罩浮现
- 表格行 hover 高亮玻璃条

### 6.6 「大气」的具体手段
- **留白**:页面间距放大(24px 起步),卡片内边距 28px+,不拥挤
- **hero 区**:仪表盘顶部一整片大数字区,一眼看到全局家底
- **克制密度**:一屏只放 3~4 张主卡,次要信息收进可展开区
- **大号数字**:数据页用 32px+ 数字排版,配淡色单位与渐变光晕

---

## 7. 数据库草案

```
admins        管理员(账号/密码hash/状态/登录信息/操作次数)
projects      项目(app_key/app_secret/名称/状态/回调/IP白名单)
card_types    套餐(名称/类型[时长|次数|永久]/时长天/次数/价格/适用范围json)
cards         卡密(卡号唯一/type_id/状态/归属项目null=通用/激活时间/次数已用/IP/来源标记/备注)
card_logs     核销流水(卡id/project_id/动作/ip/时间)
oper_logs     后台操作审计
settings      系统设置(键值)
generate_logs 生成批次记录
```

> 注意新增列:**`cards.归属项目` 默认 NULL(全局通用),激活时写入项目 ID** —— 这就是"统一池 + 单次核销"的落点。

---

## 8. 项目结构

```
十夜卡密 (卡密管理系统)
├─ server/               后端 Node.js
│  ├─ app.js             入口
│  ├─ db/                SQLite + 建表初始化
│  ├─ routes/            管理端路由 / 对外API 路由(分离)
│  ├─ services/          业务(生成/核销/签名/统计/导入)
│  ├─ middleware/        登录鉴权 / API签名 / 限频
│  └─ config.js
├─ web/                  前端 Vue3 + Vite + Element Plus
│  ├─ src/views/         登录/仪表盘/卡密/生成/套餐/项目/日志/设置
│  ├─ src/styles/        「水晶玻璃·光晕蓝紫」主题(渐变/玻璃/光效)
│  ├─ src/components/    hero数字卡/玻璃表/状态药丸/渐变按钮...
│  └─ ...
├─ docs/                 对接文档 + SDK 示例(6种语言)
└─ 方案.md
```

---

## 9. 开发计划

| 阶段 | 内容 | 验收点 |
|---|---|---|
| P1 骨架 | 项目初始化、建库、管理员登录、UI 主题落地 | 能看到"水晶玻璃·光晕蓝紫"的登录页与整体布局 |
| P2 核心 | 全局卡池列表、批量生成、套餐管理、状态操作、导入导出 | 一卡通用池能完整跑通 |
| P3 接管 | 项目管理(app_key/secret)、对外验卡 API、签名鉴权、核销流水、SDK 示例 | 任意项目可接入并验卡 |
| P4 打磨 | 仪表盘图表与动效、数据备份、部署脚本(pm2/Nginx)、对接文档 | 上云上线可直接用 |

---

## 10. 已确认 & 待确认

**✅ 已确认**
- 卡密全局统一池,**一卡通用 · 单次核销**,激活归属项目
- UI「水晶玻璃 · 光晕蓝紫」,**浅底,绝不用黑**
- 前端 Vue3 + Vite + Element Plus;数据库 SQLite;后端 Node.js + Express;云服务器公网部署

**❓ 待确认(不影响开工,但决定细节)**
1. ~~已有项目语言~~ ✅ **你已确认:都是网站** → SDK 示例以 **PHP 优先**,附 Node / Python / curl
2. 是否需要**在线卖卡/订单**模块?(纯管理+对接,还是顺带在线售卖)—— 需要就加 `orders` 表,首期默认不做
3. 想要的**规模**:大概多少张卡、多大并发?(决定要不要缓存层,十万级以内 SQLite 就够)

---

*方案确认没问题,回一句"开始",我按 P1 动工。*