# 十夜卡密 · 项目规划书

> 文档版本 v1.0 · 2026-09-13 · 状态:待评审
> 关联文档:[方案.md](方案.md)(精简版概览)
> 本规划书为完整版,涵盖目标、范围、功能、API、数据库、UI、架构、安全、部署、计划、验收。

---

## 1. 项目概述

### 1.1 项目名称
**十夜卡密** —— 综合性卡密管理系统

### 1.2 一句话定位
把多个网站项目的卡密**统一到同一个全局卡池**:一套系统完成卡密的**生成、管理、接管、对外验卡**;卡密**一卡通用**(顾客在任意接入项目里都能用,激活时归属该项目,一卡只核销一次);旧项目已有的卡密可**批量导入接管**,随后所有项目仅通过一个 HTTP API 验卡。

### 1.3 背景与痛点
- 已有多个网站项目,各自有一套卡密逻辑(存储、验卡、管理分散在各项目里),零散难维护
- 卡密无法跨项目通用,顾客想在别的项目使用时受限
- 缺少统一的后台:看不到全局总账、使用率、各项目核销占比
- 新项目接入要重新造轮子

### 1.4 目标
| # | 目标 | 量化标准 |
|---|---|---|
| G1 | 全局统一卡池 | 所有项目共用一张总表,新增项目**不需要迁移数据**,开个项目参数即可接入 |
| G2 | 一卡通用 | 顾客可用同一张卡在任意适用项目激活;激活即归属,一卡单次核销 |
| G3 | 无缝接管 | 旧项目卡密可一键导入,验卡逻辑最小改动(只改一处)接入 |
| G4 | 精美后台 | 「水晶玻璃·光晕蓝紫」浅色炫酷 UI,真实性图标,非黑色、非纯白 |
| G5 | 公网可用 | 云服务器部署,HTTPS,验卡 API 稳定、防重放、可限频 |

---

## 2. 项目范围

### 2.1 范围内(首期 P0+P1 功能)
全局卡池管理、卡密批量生成、套餐(卡类型)管理、**验卡/核销 API 与签名鉴权**、项目管理(接入授权)、旧卡导入/导出、操作与核销日志、统计仪表盘、管理员登录与多管理员、系统设置/备份、对接文档与 SDK 示例。

### 2.2 范围外 / 后置(P2 及以后,不影响首期上线)
- 在线**卖卡/订单/支付**模块(待老板确认;如需,追加 `orders` 表)
- 第三方发卡平台**自动同步**(当前以"文件导入"覆盖)
- **沙箱测试环境**(给项目方联调用,首期用"正式库+测试卡"代替)
- 多语言、皮肤切换

### 2.3 假设与默认决策
- 规模假设:**十万级卡密、验卡 QPS < 100** → SQLite + 内存缓存足够,不引入 Redis(预留抽象层,可后加)
- 数据不跨进程写,后端单进程即可承载
- 不接入支付/短信等外部服务(留扩展点)

---

## 3. 用户角色

| 角色 | 说明 | 权限 |
|---|---|---|
| 超级管理员 | 系统所有者 | 全部功能 + 管理员管理 + 危险操作(删除/重置密钥) |
| 管理员 | 日常运营 | 卡密增删改查/生成/导入/导出、套餐、项目、日志可查看,不可管理管理员、不可危险操作(可配置) |
| 项目方(外部) | 各网站的**后端服务**,不是真人登录 | 仅通过 API 凭证(`app_key/app_secret`)调验卡接口,无后台权限 |

---

## 4. 核心业务规则

### 4.1 卡密状态机
```
生成 → 未使用
未使用 → 已使用   (顾客在某项目激活成功;写 归属项目/激活时间/过期时间)
已使用 → 已过期   (时效耗尽或次数耗尽,由验卡时判定/定时任务标记)
未使用/已使用 → 已禁用   (管理员禁用,防继续使用)
已禁用 → 未使用/已使用   (管理员解禁)
任意 → 已作废     (管理员主动作废,不参与核销)
```

### 4.2 一卡单次核销(核心规则)
- 卡生成时**不绑定项目**,属于"全局池 + 套餐"
- 激活时:校验通过 → 归属写入"当前激活项目",一卡只成功激活一次
- 已使用卡拿到**其他项目**验卡 → 返回错误 `已绑定项目X,不可在其他项目使用`
- 已使用卡在**归属项目**内、有效期内的验卡/扣次数 → 正常返回剩余值

### 4.3 套餐适用范围
- 套餐默认"全部项目可用";可配置"仅指定项目可用"
- 适用范围在**激活时校验**,与验卡一致

### 4.4 并发与防重
- 激活采用**原子更新**:`UPDATE cards SET status='已使用',... WHERE 卡号=? AND status='未使用'`,受影响行数=0 即被他人抢先 → 返回"已被使用"
- better-sqlite3 事务包裹关键操作,SQLite 开启 WAL

### 4.5 卡号规则
- 字符集默认 `23456789ABCDEFGHJKLMNPQRSTUVWXYZ`(剔除易混 `0O1lI`),可自定义
- 长度默认 16,可自定义;可加前缀(如 `SY2026XXXX`)
- 可选"校验位":字符串整体按固定算法加 1 位校验字符,前端先本地校验再请求,防止猜号刷接口
- 全局唯一索引兜底 + 生成时去重重试

### 4.6 时长/次数/永久
- **时长卡**:激活时按套餐天数计算过期时间;验卡返回剩余天数(取整或带小时)
- **次数卡**:激活后按次扣费;`consume` 每次携带扣减数,剩余不足则拒绝并保持事务一致
- **永久卡**:无过期时间,次数不限

---

## 5. 功能需求明细

> 优先级 P0=首期必做 / P1=首期做 / P2=二期
> 编号缩写:登录 LOG / 仪表盘 DSH / 卡密 CRD / 生成 GEN / 套餐 TYP / 项目 PRJ / 导入 IMP / 导出 EXP / API / 日志 LOG / 设置 SET

### 5.1 登录与权限 (LOG)
| 编号 | 需求 | 优先级 |
|---|---|---|
| LOG-1 | 管理员登录,密码 bcrypt 存储,登录态 JWT(cookie/httpOnly) | P0 |
| LOG-2 | 登录失败锁定:连续 5 次失败锁 15 分钟;可选图形验证码 | P0 |
| LOG-3 | 两位角色(超管/管理员)权限控制,前端按钮 + 后端中间件双重控制 | P0 |
| LOG-4 | 登录日志(成功/失败/IP/UA) | P0 |

### 5.2 仪表盘 (DSH)
| 编号 | 需求 | 优先级 |
|---|---|---|
| DSH-1 | hero 大数字区:总卡密、未使用、已使用、已过期、已禁用;今日生成、今日核销 | P0 |
| DSH-2 | 全局使用率进度条、项目核销占比 TOP5、最近核销动态列表 | P0 |
| DSH-3 | 近 7/30 天"生成 vs 消耗"渐变面积图 | P1 |
| DSH-4 | 数字滚动 + 光晕卡片动效 | P1 |

### 5.3 卡密管理 (CRD) —— 全局总表
| 编号 | 需求 | 优先级 |
|---|---|---|
| CRD-1 | 列表:卡号、套餐、状态、归属项目(未使用=空)、创建/激活时间、备注,分页(≥ 每页 100) | P0 |
| CRD-2 | 筛选:套餐、状态、归属项目、时间范围;关键词搜索(卡号前缀) | P0 |
| CRD-3 | 操作:禁用/启用、作废、重置(已使用回退未使用,需二次确认)、批量删除(二次确认) | P0 |
| CRD-4 | 卡详情抽屉:完整字段 + 核销流水 + 操作历史 | P1 |

### 5.4 卡密生成 (GEN)
| 编号 | 需求 | 优先级 |
|---|---|---|
| GEN-1 | 参数:数量、前缀、长度、字符集、套餐、批次备注 | P0 |
| GEN-2 | 生成前预览样卡、生成上限校验(单批 ≤10000,分批事务写入) | P0 |
| GEN-3 | 生成结果页:展示/下载/复制本批卡密;可选"仅管理员可再次导出" | P0 |
| GEN-4 | 生成批次日志(谁/何时/规则/数量) | P0 |

### 5.5 套餐管理 (TYP)
| 编号 | 需求 | 优先级 |
|---|---|---|
| TYP-1 | 套餐 CRUD:名称、类型(时长/次数/永久)、时长天、次数、价格(可选)、状态 | P0 |
| TYP-2 | 适用范围:全部项目 / 勾选指定项目 | P0 |
| TYP-3 | 套餐修改后对存量卡即时生效(卡算套餐于验卡时实时解析) | P0 |
| TYP-4 | 套餐被使用时禁止删除(软删除/置停用) | P0 |

### 5.6 项目管理 (PRJ) —— 只管接入授权
| 编号 | 需求 | 优先级 |
|---|---|---|
| PRJ-1 | 项目 CRUD:名称、状态、备注、回调地址 | P0 |
| PRJ-2 | 自动生成 `app_key`(公开)与 `app_secret`(仅展示一次,可复制) | P0 |
| PRJ-3 | 密钥重置轮换(旧密钥立即失效,二次确认) | P0 |
| PRJ-4 | 可选 IP 白名单、每项目限频阈值 | P1 |
| PRJ-5 | 项目冒烟测试:后台内置"试一发"模拟验卡请求 | P1 |

### 5.7 导入 / 导出 (IMP/EXP)
| 编号 | 需求 | 优先级 |
|---|---|---|
| IMP-1 | 导入 TXT(一行一卡)/ CSV(卡号,套餐,状态,备注) | P0 |
| IMP-2 | 选择目标套餐;全局去重——重复卡号跳过并**逐行报告**(成功 N 失败 M 明细可下载) | P0 |
| IMP-3 | 支持带"已使用 + 归属项目 + 激活时间"导入(旧系统无损迁移) | P1 |
| IMP-4 | 导出:按筛选条件导出;选择 TXT/CSV;敏感列(如仅卡号)可选 | P0 |

### 5.8 对外验卡 API
| 编号 | 需求 | 优先级 |
|---|---|---|
| API-1 | `verify / activate / consume / query / freeze / unfreeze` 六接口 | P0 |
| API-2 | HMAC-SHA256 签名鉴权 + 时间戳防重放 + nonce | P0 |
| API-3 | 统一响应 `{code,message,data}` + 错误码字典 | P0 |
| API-4 | 项目维度限频 + 可选 IP 白名单 | P1 |
| API-5 | 激活成功可选回调项目 URL(失败重试 3 次) | P1 |
| API-6 | SDK 示例:PHP(优先)/ Node / Python / curl | P0 |

### 5.9 日志与安全 (LOG/SET)
| 编号 | 需求 | 优先级 |
|---|---|---|
| LOG-5 | 后台操作审计(谁/何时/动作/对象) | P0 |
| LOG-6 | 核销流水(卡/项目/动作/IP/时间),卡详情可反查 | P0 |
| SET-1 | 管理员 CRUD、修改个人密码 | P0 |
| SET-2 | SQLite 一键备份/下载/恢复 | P0 |
| SET-3 | 站点名/公告/安全参数(锁定阈值、时间窗) | P0 |
| SET-4 | 数据导出(全部卡密明文 + 流水)离线归档 | P1 |

---

## 6. 对外 API 详细设计

### 6.1 鉴权
- 每个项目持 `app_key`(公开)与 `app_secret`(私有)
- 每次请求携带 Header:
  - `X-App-Key`: app_key
  - `X-Timestamp`: Unix 秒
  - `X-Nonce`: 本次请求随机串(≤ 32 位)
  - `X-Sign`: 签名
- 签名算法(**HMAC-SHA256**):
  ```
  sign = HMAC_SHA256( app_secret, app_key + "\n" + timestamp + "\n" + nonce + "\n" + sha256(body) )
  sign 转十六进制小写
  ```
- 服务端校验:时间戳 ±300s 内、nonce 5min 内未重放(内存集+DB 兜底)、验签通过、限频通过 → 放行
- 备注:所有请求用 POST + JSON body,避免 URL 长度与缓存问题

### 6.2 接口
| 接口 | 方法 | 请求体 | 响应 data |
|---|---|---|---|
| `/api/v1/card/verify` | POST | `{card}` | `{valid, type, remaining_days, remaining_times, bound_project}` |
| `/api/v1/card/activate` | POST | `{card}` | `{type, expire_at, remaining_times, project_id}` |
| `/api/v1/card/consume` | POST | `{card, times}` | `{remaining_times, consumed}` |
| `/api/v1/card/query` | POST | `{card}` | 卡详情+剩余+归属+状态 |
| `/api/v1/card/freeze` | POST | `{card}` | `{status}` |
| `/api/v1/card/unfreeze` | POST | `{card}` | `{status}` |
| `GET /api/v1/status` | GET | - | 服务健康检查(限频内) |

### 6.3 错误码字典(节选)
| code | message | 说明 |
|---|---|---|
| 0 | ok | 成功 |
| 1001 | 签名无效 | 验签失败 / 时间戳过期 / nonce 重放 |
| 1002 | 接口调用过于频繁 | 限频触发 |
| 1003 | 项目已被禁用 | 项目状态检查 |
| 2001 | 卡号不存在 | - |
| 2002 | 卡密已被使用 | - |
| 2003 | 卡密已过期 | - |
| 2004 | 卡密已被禁用 | 管理端禁用 |
| 2005 | 卡密已作废 | - |
| 2006 | 卡已绑定其他项目 | 一卡单次核销规则 |
| 2007 | 该卡在项目不可用 | 套餐适用范围 |
| 2008 | 次数不足 | consume 场景 |
| 2009 | 卡已被冻结 | freeze 状态 |

### 6.4 示例(PHP,优先生成)
```php
$sign = hash_hmac('sha256', $app_key."\n".$timestamp."\n".$nonce."\n".hash('sha256', $body), $app_secret);
// curl POST https://api.你的域名.com/api/v1/card/activate
```
> 规划书交付后同步产出完整可复制示例:PHP / Node(PHP 网站多优先),(可选)Python / curl。

---

## 7. 数据库详细设计

> SQLite,UTF-8,WAL。主键统一 `id INTEGER PRIMARY KEY AUTOINCREMENT`,时间统一 `INTEGER`(Unix 秒)或 `TEXT`(ISO),规划定为 `INTEGER`。

### 7.1 `admins` 管理员
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | - |
| username | TEXT UNIQUE NOT NULL | 登录名 |
| password | TEXT NOT NULL | bcrypt hash |
| role | TEXT | `super` / `admin` |
| status | INTEGER | 1 启用 0 禁用 |
| login_fail_count | INTEGER | 失败次数 |
| lock_until | INTEGER | 锁定截止(0=无) |
| last_login_at / last_login_ip | INTEGER/TEXT | 最近登录 |
| created_at | INTEGER | 创建时间 |

### 7.2 `projects` 项目
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | - |
| name | TEXT NOT NULL | 项目名 |
| app_key | TEXT UNIQUE NOT NULL | 公开密钥 |
| app_secret | TEXT NOT NULL | 私有密钥(界面仅展示一次) |
| status | INTEGER | 1 启用 0 禁用 |
| callback_url | TEXT | 激活成功回调 |
| ip_whitelist | TEXT | 逗号分隔,空=不限制 |
| rate_limit | INTEGER | 每项目每分钟限频,默认 60 |
| remark | TEXT | 备注 |
| created_at | INTEGER | - |

### 7.3 `card_types` 套餐
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | - |
| name | TEXT NOT NULL | 如「30天卡」 |
| kind | TEXT | `duration` / `times` / `permanent` |
| days | INTEGER | 时长卡的天数 |
| times | INTEGER | 次数卡的次数 |
| price | REAL | 可选售价 |
| scope_projects | TEXT | JSON 数组;`"[]"`=全部项目 |
| status | INTEGER | 1 启用 0 停用 |
| sort | INTEGER | 排序 |
| created_at | INTEGER | - |

### 7.4 `cards` 卡密(全局池,核心)
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | - |
| card | TEXT UNIQUE NOT NULL | 卡号(全局唯一) |
| type_id | INTEGER NOT NULL | → card_types.id |
| status | TEXT | `unused` / `used` / `expired` / `disabled` / `void` |
| bound_project_id | INTEGER NULL | **激活时写入;NULL=未使用/通用** |
| activated_at | INTEGER NULL | 激活时间 |
| expire_at | INTEGER NULL | 过期时间(时长卡) |
| times_used | INTEGER 默认0 | 次数卡已用 |
| last_verify_at | INTEGER NULL | 最近验卡时间 |
| last_ip | TEXT | 上次操作 IP |
| source | TEXT | 导入来源标记(接管的旧项目名) |
| remark | TEXT | 备注 |
| created_at | INTEGER | - |
| 索引 | - | `(status)`,`(type_id)`,`(bound_project_id)`;UNIQUE(card) |

### 7.5 `card_logs` 核销流水
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | - |
| card_id | INTEGER | → cards.id |
| project_id | INTEGER | 操作项目 |
| action | TEXT | `activate` / `verify` / `consume` / `freeze` / `unfreeze` |
| detail | TEXT | 附加(剩余值等) |
| ip | TEXT | 调用方 IP |
| created_at | INTEGER | - |
| 索引 | - | `(card_id)`,`(project_id)`,`(action)` |

### 7.6 `oper_logs` 后台审计
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | - |
| admin_id | INTEGER | 操作人 |
| action | TEXT | 模块:动作 |
| target | TEXT | 对象 |
| detail | TEXT | 摘要 |
| ip | TEXT | - |
| created_at | INTEGER | - |

### 7.7 `settings` 系统设置
`key TEXT PK` / `value TEXT` —— 站点名、公告、安全参数、套餐校验位开关等。

### 7.8 `generate_logs` 生成批次
| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | - |
| admin_id / admin_name | INTEGER/TEXT | 生成人 |
| type_id | INTEGER | 套餐 |
| count | INTEGER | 数量 |
| rule | TEXT | 规则 JSON(前缀/长度/字符集/校验位) |
| batch_no | TEXT | 批次号 |
| created_at | INTEGER | - |

### 7.9 `nonces` 防重放记录
`nonce TEXT PK`, `expire_at INTEGER` —— 定期清理过期。

---

## 8. UI / UX 设计规范 ——「水晶玻璃 · 光晕蓝紫」

### 8.1 设计三原则
1. **浅·彩**:页面底色是淡彩(微蓝紫),**不是纯白**,更不是黑
2. **光**:氛围来自光 —— 青/紫/粉柔光斑、渐变描边、霓虹光晕 → 炫酷
3. **透**:水晶玻璃承载信息,磨砂 + 半透明白卡 + 1px 白边 + 彩色投影 → 精致大气

### 8.2 配色(精确值)
| 角色 | 色值 | 用途 |
|---|---|---|
| 页面底色 | `#eef1ff → #e3ecff` 斜向渐变 + 微光纹 | **整体背景,淡彩而非纯白** |
| 主渐变 | 青 `#22d3ee` → 蓝 `#4f7cff` → 紫 `#8b5cf6` | Logo/按钮/顶栏光条/图表 |
| 霓虹光晕 | `#38bdf8`/`#a78bfa`/`#fb7185` 三柔光斑 | 面板背后浮动光,blur(80px) |
| 玻璃面 | `rgba(255,255,255,.66)` + blur(20px) | 卡片/顶栏/弹窗 |
| 主文字 | `#1e2b4f` | 标题正文(深蓝墨) |
| 次文字 | `#64748b` | 表头说明 |
| 成功/警告/危险 | `#10b981`/`#f59e0b`/`#ef4444` | 状态与提示 |
| 状态药丸 | 各状态渐变小底色 + 同色系文字 | 未使用(青)/已使用(蓝)/过期(灰紫)/禁用(红) |

### 8.3 图标规范 ——「真实性图标」
- **不用细线条扁平图标**(Element Plus 缺省图标仅作兜底),统一使用**渐变立体质感图标**
- 图标风格:双色渐变填充(青蓝→紫)、**微浮雕高光**、柔和彩色投影、圆润线条,接近"立体真实"质感
- 实现:全部 SVG(保证任何 DPI 清晰),用 `linearGradient + filter: drop-shadow`,封装成 `<Icon>` 组件,统一渐变角度与线宽(2px 内描边 + 渐变填充)
- 覆盖范围:
  - 导航模块图标 ×9(仪表盘/卡密/生成/套餐/项目/导入导出/日志/设置/文档)
  - 数据卡图标(总量/使用率/今日生成/在线项目)
  - 状态徽标(✓ 已用/⏱ 未用/◇ 过期/⛔ 禁用/警示)
  - 操作图标(禁用/解禁/作废/重置/下载/导入/复制/搜索)
- hover 效果:图标轻微上浮 + 光晕;选中态:渐变底 + 白图标

### 8.4 布局
- **登录页**:全屏淡彩渐变 + 漂移光斑;居中玻璃登录卡 + 大牌字「十夜卡密」;渐变主按钮
- **主框架**:顶部**玻璃导航条**(左侧品牌光标 + 导航,选中项为渐变胶囊亮条)
- **仪表盘**:顶部 hero 玻璃大数字区(总/未用/已用/过期/禁用 + 今日生成/今日核销),下方玻璃卡片网格(趋势图/项目占比/动态流水)
- **业务页**:统一玻璃表卡 + 顶部工具条(筛选/搜索/主操作渐变按钮);详情抽屉/弹窗磨砂遮罩
- **大气手段**:页面间距 24px+,卡片内边距 28px+;hero 数字 32px+;一屏 ≤4 张主卡,次要信息折叠

### 8.5 动效目录
数字滚动、卡片 hover 上浮+光晕、背景光斑 10s 漂移、渐变描边呼吸、页面淡入、弹窗磨砂浮现、表格行 hover 玻璃条。**原则:克制、均衡、不打扰。**

### 8.6 字体
- 中文:`PingFang SC` / `HarmonyOS Sans SC` / `Microsoft YaHei` 回退栈
- 数字/英文:`DIN Alternate`(大数字场景)+ tabular-nums,强化"数据感"

---

## 9. 技术架构

### 9.1 选型
| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | Node.js ≥ 18 + Express | 与前端同语言;API 密集高性能;生态成熟 |
| 数据库 | SQLite + better-sqlite3 | 单文件、WAL、事务、百万级内足够 |
| 前端 | Vue3 + Vite + Element Plus | 组件齐;二次定制主题成熟 |
| 鉴权 | JWT(httpOnly cookie)+ bcrypt | 后台;外部 API 用 HMAC 签名 |
| 部署 | pm2 + Nginx(HTTPS)+ 云服务器 | 单进程跑全站,Vite 产物由后端托管 |

### 9.2 目录结构
```
shiyeka/
├─ server/                后端
│  ├─ app.js               入口(挂载中间件/静态站)
│  ├─ config.js            配置文件(端口/密钥/JWT/路径)
│  ├─ db.js                建库/建表/迁移
│  ├─ routes/admin/        管理端 API
│  ├─ routes/open/         对外验卡 API
│  ├─ services/            业务(genCards/verify/activate/consume/import/export/stats)
│  ├─ middleware/          auth.admin / auth.sign / ratelimit / errorHandler
│  ├─ lib/                 crypto.js(签名/卡号/校验位) logger.js
│  └─ data/                SQLite 文件 + backups/
├─ web/                    前端 Vue3
│  ├─ src/views/           登录/仪表盘/卡密/生成/套餐/项目/导入/日志/设置/文档
│  ├─ src/components/      GlassCard/HeroStat/GlowButton/StatusPill/IconFrame/...
│  ├─ src/styles/          tokens.css(色板) glass.css(玻璃/光效) glow.css(渐变)
│  ├─ src/lib/             api.js request.js
│  └─ vite.config.js
├─ docs/                   对接文档 + SDK 示例(PHP/Node/Python/curl)
├─ deploy/                 pm2.config.js nginx.conf 部署说明.md
└─ 规划书.md · 方案.md
```

### 9.3 安全设计
- 后台:bcrypt 存密、登录锁定、JWT httpOnly、操作鉴权中间件、防 SQL 注入(参数绑定)、输出转义防 XSS、CSP 头
- 对外:HMAC 签名 + 时间窗 + nonce 防重放、项目限频、可选 IP 白名单、统一错误(不泄露内部信息)
- 关键写操作原子事务;卡号唯一约束兜底

### 9.4 数据备份
- SQLite 使用 `VACUUM INTO` 热备份(不锁库),后台一键下载
- 建议部署端 cron 每日备份到 data/backups,保留 7 天
- 恢复:上传备份文件执行恢复(或手动替换文件后重启,规划提供界面操作)

---

## 10. 部署方案

### 10.1 生产拓扑(云服务器,公网)
```
用户浏览器 ── HTTPS ──> Nginx(443) ── / ──> Node(pm2, :1111)
项目网站后端 ── HTTPS ──> Nginx ── /api/v1/* ──> Node(:1111)  ← 验卡
                                        │
                                   SQLite 单文件(本地磁盘)
```
- Node 用 pm2 常驻(自动重启、日志);Nginx 托管证书与反代;Vite 构建产物由 Node `express.static` 托管,`/api` 与页面同源,简化 CORS
- 环境变量:`PORT`、`JWT_SECRET`、`DB_PATH`、`SESSION_TTL`、`RATE_LIMIT_DEFAULT` 等全部走 config + .env

### 10.2 上线清单
1. 服务器安装 Node ≥18、pm2、Nginx 与证书
2. `npm install && npm run build`(web)→ 服务端托管
3. 初始化数据库(`node server/db.js --init`),创建超管
4. pm2 启动,nginx 反代与 HTTPS
5. 冒烟:健康检查、创建项目→SDK 示例验一卡

---

## 11. 里程碑与迭代计划

> 每阶段独立可交付、可验收;采用小迭代:先可见、后完整。

### P1 骨架与主题(预计 1 周)
任务:`admin` 登录/锁定/JWT → 建库建表 → 玻璃玻璃主题组件库(tokens/GlassCard/HeroStat/GlowButton/IconFrame 渐变图标) → 登录页 → 主框架(玻璃导航/路由/权限) → 系统设置(站点名/公告) 
**验收**:登录进入后台,浅彩玻璃 UI + 渐变图标落地,非白非黑;设置可改站点名。

### P2 核心闭环(预计 1.5~2 周)
任务:套餐 CRUD → 卡密全局列表(筛选/搜索/分页) → 批量生成(规则/去重/批次) → 状态操作(禁用/作废/重置/删除) → 导入(去重报告) → 导出 → 卡详情与流水 → 仪表盘(hero + 静态图表)
**验收**:新卡从生成到使用状态流转闭环;旧卡导入去重正确;仪表盘数字准确。

### P3 接管与 API(预计 1~1.5 周)
任务:项目 CRUD 与密钥 → 对外六接口 + HMAC 签名/防重放/限频 → 核销流水落库 → 回调(可选) → SDK 示例(PHP 优先)与对接文档 → 项目冒烟测试页
**验收**:用一个模拟"网站项目"仅改一处代码完成验卡/激活,一卡单次核销跨项目拦截正确;重放攻击被拒。

### P4 打磨与上线(预计 1 周)
任务:趋势图/占比图 + 动效细化 → 日志审计页完善 → 备份/恢复界面 → 部署(pm2/Nginx/HTTPS)与上线清单 → 全量验收与文档
**验收**:按部署清单在云服务器跑通,公网 HTTPS 验卡稳定,P0/P1 需求全部达标。

---

## 12. 测试计划

| 类别 | 覆盖 |
|---|---|
| 功能 | 各模块正向/反向用例;状态机全路径流转 |
| 边界 | 批量上限、超长卡号、字符集合法、剩余次数=0 |
| 并发 | 同卡并发激活只成功一次(原子更新);并发 generate 无重复卡号 |
| 安全 | 签名篡改/重放/过期、限频压测、XSS/SQL 注入、越权(admin vs super) |
| 兼容 | 前端 Win/Chrome、Edge、移动端浏览器可正常缩放;对外接口 HTTP/1.1 普通 POST 均可调 |
| 性能 | 10 万卡查询 P95 < 200ms;QPS 100 验卡事务正确 |

---

## 13. 文档计划
- `docs/对接文档.md`(含签名算法、六接口、错误码、PHP/Node/Python/curl 示例)
- `deploy/部署说明.md`(服务器初始化到上线的完整命令)
- 后台内置「对接文档中心」页(P1 落地静态、P3 对接参数自动注入)

---

## 14. 风险与对策
| 风险 | 影响 | 对策 |
|---|---|---|
| 一卡多项目并发核销 | 金额损失/滥用 | 原子 UPDATE + 唯一状态机;测试覆盖 |
| 重放攻击/签名泄露 | 接口被刷 | 时间窗+nonce+限频+密钥重置,泄露即轮换 |
| 项目方调试困难 | 接入慢 | PHP 示例优先 + 冒烟测试页 + 联调指引 |
| SQLite 写入锁 | 高并发写瓶颈 | 写少读多场景天然适配;WAL;预留切 MySQL 抽象 |
| UI 效果达不到预期 | 返工 | 先做主题组件与登录页,你确认风格后再铺所有页面 |

---

## 15. 验收与交付物(总计)
- 可运行的全栈系统(后台 + 对外 API)+ 初始化脚本
- SQLite 数据库(空库 + 演示数据可选)
- 对接文档 + PHP/Node/Python/curl 示例
- 部署目录(pm2/nginx/说明)
- 「水晶玻璃·光晕蓝紫」完整主题与渐变图标组件库

---

## 16. 待老板确认(不影响开工)
1. **是否需要在线卖卡/订单/支付模块**?(默认首期不做,做"纯管理+对接")
2. 后台是否需要**演示数据**(假卡密/假流水)用于看效果?(默认要,可一键清空)
3. 项目网站主要以 **PHP** 为主吗?(默认按 PHP 优先写 SDK 示例)

---

*规划书完。回复「开始」即按 P1 动工,或指出需调整之处。*